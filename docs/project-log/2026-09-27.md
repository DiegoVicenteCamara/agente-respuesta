# Bitácora — Semana del 2026-09-21 al 2026-09-27

## Página de estado
- **Fecha de cierre**: 2026-09-27
- **Resumen**: El grafo de organización de subagentes queda como un flujo continuo de ramas tipo GitHub (curvas, escala constante, columnas reutilizables) y se instituye la documentación obligatoria vía ADR + bitácora semanal.
- **Avance acumulado**: 2 / 2 entregables planificados del periodo.

## Logros del periodo
- Evento `subtask_started`, timestamp `ts` y campo `subtask` en el payload del bus (`backend/orchestrator/graph.py`) + tests 12/12 verdes.
- Grafo de la web (`web/index.html`) rediseñado 3 veces en el periodo: filas por tarea → grafo único continuo → **curvas S/U con columnas reutilizables por tarea** (columna libre más próxima, liberación al terminar; `W` fijo = escala constante, sin encoger).
- Verificación automática del grafo con harness Node (15/15 asserts: columnas, commits, splines `Q`, `viewBox`) y ciclo E2E real `plan_ready → subtask_started → subtask_done → analysis_ready`.
- GitHub CLI instalado y autenticado (`DiegoVicenteCamara`); issue #1 creada y rama `feat/gitgraph-agents`.
- Documentación instituida: `docs/decisions/ADR-001...` (decision-log) + regla de documentación obligatoria en `AGENTS.md` + esta bitácora (project-log).

## Desvíos o Bloqueos
- **Definición de la forma de rama** — Retrasado por: el modelo de la sesión no soporta imágenes (error de clipboard), hubo que confirmar la forma vía preguntas de texto en lugar de la captura de referencia del usuario.
- **Commit del conjunto** — Pendiente: los cambios (`web/index.html`, `AGENTS.md`, `docs/`) están sin commitear en `feat/gitgraph-agents` al cierre de la sesión.

## Lecciones aprendidas
- **Lección**: sin soporte de imágenes, las decisiones visuales quedan atadas a la conversación y se pierden si se resume mal.
  **Ajuste táctico**: desde ahora toda decisión visual con trade-offs se documenta en un ADR en `docs/decisions/` (con la skill `decision-log`) antes o junto al commit que la implementa.
- **Lección**: el pool global de lanes crecía el `viewBox`, encogiendo el gráfico con cada tarea nueva (causa no evidente sin medir `W`).
  **Ajuste táctico**: validar con el harness Node las invariantes geométricas (`W` constante, conteo de nodos) al tocar el render para detectar regresiones de escala.

## Bloqueos que continúan
- Commit pendiente de los cambios en `feat/gitgraph-agents` (se ofrecerá a cierre de la siguiente sesión).